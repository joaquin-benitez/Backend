<div>
  {{#if isAdmin}}
    <ul>
      {{#each foods}}
        <li>{{name}}: {{price}}</li>
      {{/each}}
    </ul>
  {{else}}
    <div>Usuario no autorizado</div>
  {{/if}}
</div>
<script src="/js/index.js"></script>